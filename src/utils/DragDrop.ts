/* import Sortable from 'sortablejs/modular/sortable.core.esm.js'

export default function sortableDrag () {
  const itemDrag = document.querySelector('.itemDrag')

  if (itemDrag) {
    Sortable.create(itemDrag, {
      animation: 150
    })
  }
} */
